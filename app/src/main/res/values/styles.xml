<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme -->
    <style name="AlternativeTheme" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary colors -->
        <item name="colorPrimary">@color/material_dynamic_neutral_variant40</item>
        <item name="colorPrimaryVariant">@color/material_dynamic_neutral_variant40</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Window animations for showing/hiding -->
        <item name="android:windowEnterAnimation">@android:anim/slide_in_left</item>
        <item name="android:windowExitAnimation">@android:anim/slide_out_right</item>
    </style>

</resources>
